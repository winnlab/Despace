<div class="projects">
    <div class="projectsWrapper">

        <div class="btn-project btn-proj-t"></div>
        <div class="btn-proj-b"></div>

        {{#each projects}}
            <project class="{{getClassName @index}} {{direction}}" images="{img}" gid="{@index}" ></project>
        {{/each}}
    </div>
</div>
