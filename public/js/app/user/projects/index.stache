<div class="projects">
    <div class="projectsWrapper">
        {{#if visibleBtn}}
        <div class="btn-project btn-proj-t"></div>
        <div class="btn-proj-b"></div>
        {{/if}}
        {{#each projectsPage}}
            <project class="item-project {{getClassName @index}} {{direction}}" images="{img}" gid="{@index}" ></project>
        {{/each}}
    </div>
</div>
